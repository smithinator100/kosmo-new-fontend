/**
 * Shared cross-component types.
 */

/**
 * Lucide icon name. Kept as a structural string for now so the library
 * doesn't depend on Lucide's full enum at type-check time.
 */
export type LucideIconName = string;
